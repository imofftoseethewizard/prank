Objective: describe and explain design of memory representation and management

fundamental objects
  values
    4 byte with tag in lowest 3 bits
    types of values
  pairs
    two values
  blocks
    a two value header plus 0-n values
  block owners
    pair, block type and block addr
    types of blocks

object operations
  alloc
  dealloc
  make

intrinsically mananged pairs
explicitly managed pairs

management structures
  pair flags
  pair group flags
  pair free list
  separated pair list
  released pair list
  block free list
  ref index
  indexes
    bytearrays
    cursors
    empty entries

management operations
  extending pair storage
  relocating blocks
  extending block storage
  trace separated
  trace reachable
  trace/dealloc separated
  compact block storage
  compact block free list
  gc-step

garbage collection
  general description
  phase description
  gc interface
